---
id: "java/basics/variables_and_types#w1"
title: "変数に値を代入できる"
type: "WRITE"
difficulty: "Easy"
format: "fillInBlank"
topicId: "variables_and_types"
category: "java/basics"
fillInBlankAnswers:
  blank1: "int"
  blank2: "25"
explanation: |
  変数を宣言するには、まず **型**（この場合は`int`）を指定し、次に変数名、そして`=`で値を代入する。

  ```java
  int age = 25;
  ```

  - `int`: 整数型の型名
  - `age`: 変数名
  - `25`: 代入する値
---

import { BlankInput } from '@site/src/components/question/inputs/BlankInput';

## 問題

以下のコードの空欄を埋めて、変数`age`に値`25`を代入してください。

```java
```
<BlankInput id="blank1" /> age = <BlankInput id="blank2" />;
```
```
