---
id: \"java/basics/03a_scanner#string_vs_int_reading\"
title: "斁E���E読み取りと整数読み取りの違いを説明できる"
type: "KNOW"
difficulty: "Easy"
format: "multipleChoice"
topicId: "03a_scanner"
category: "java/basics"
multipleSelect: false
choices:
  - id: "A"
    text: "nextLine()は1行�E体を斁E���Eとして読み取り、nextInt()は整数のみを読み取る"
  - id: "B"
    text: "nextLine()は数値のみ読み取り、nextInt()は斁E���Eを読み取る"
  - id: "C"
    text: "nextLine()とnextInt()は全く同じ動作をする"
  - id: "D"
    text: "nextLine()はファイルから読み取り、nextInt()はキーボ�Eドから読み取る"
answers:
  correct: ["A"]
explanation: |
  **nextLine()とnextInt()の違い**�E�E

  | メソチE�� | 読み取る冁E�� | 戻り値の垁E|
  |----------|-------------|-----------|
  | `nextLine()` | 1行�E体を斁E���Eとして読み取る | String |
  | `nextInt()` | 整数のみを読み取る | int |
  | `nextDouble()` | 小数のみを読み取る | double |

  **重要な違い**�E�E
  - `nextLine()`は**改行文字まで**読み取り、改行文字を消費する
  - `nextInt()`は**数値のみ**を読み取り、改行文字�E残る

  ```java
  Scanner scanner = new Scanner(System.in);

  // "25"と入力してEnter
  int age = scanner.nextInt();  // 25が読み取られる�E�改行文字�E残る�E�E

  // "田中太郁Eと入力してEnter
  String name = scanner.nextLine();  // "田中太郁Eが読み取られる
  ```
---

## 問顁E

nextLine()とnextInt()の違いとして最も適刁E��も�Eを選べ、E

## 解説

nextLine()とnextInt()は、E*読み取るチE�Eタの種類が異なめE*、E

**nextLine()の使ぁE��**�E�E
- 名前めE��所など、文字�E全体を読み取る
- 数字と斁E��が混ざった�E力（例：、E23abc」！E
- 空白を含む斁E���E�E�例：「田中 太郎」！E

**nextInt()の使ぁE��**�E�E
- 年齢めE��量など、整数のみを読み取る
- 計算に使ぁE��値
- ユーザーの選択肢�E�E, 2, 3など�E�E

import { OneCompilerCodeBlock } from '@site/src/components/OneCompilerCodeBlock';

<OneCompilerCodeBlock language="java" code={`import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // 斁E���Eを読み取る侁E
        System.out.print("あなた�E名前を�E力してください: ");
        String name = scanner.nextLine();
        System.out.println("名前: " + name);

        // 整数を読み取る侁E
        System.out.print("あなた�E年齢を�E力してください: ");
        int age = scanner.nextInt();
        System.out.println("年齢: " + age);

        // 小数を読み取る侁E
        System.out.print("あなた�E身長を�E力してください(m): ");
        double height = scanner.nextDouble();
        System.out.println("身長: " + height + "m");
    }
}`} />

**よくあるエラー**�E�E
```java
int age = scanner.nextInt();  // "25"と入劁E
scanner.nextLine();  // 改行文字を消費�E�これを忘れると次のnextLine()が空斁E���Eを返す�E�E
String name = scanner.nextLine();  // "田中太郁Eと入劁E
```

こ�Eように、`nextInt()`の後に`nextLine()`を使ぁE��合�E、E��に`scanner.nextLine()`を挟んで改行文字を消費する忁E��がある、E
