---
id: \"java/basics/03a_scanner#prepare_scanner\"
title: "標準�E力を使ぁE��備ができる"
type: "WRITE"
difficulty: "Easy"
format: "fillInBlank"
topicId: "03a_scanner"
category: "java/basics"
fillInBlankAnswers:
  blank1: ["import java.util.Scanner;", "import java.util.Scanner", "import  java.util.Scanner;"]
  blank2: ["Scanner", "scanner"]
  blank3: ["new Scanner(System.in)", "new Scanner(System.in);", "new  Scanner(System.in)"]
sampleAnswer: "Scanner scanner = new Scanner(System.in);"
explanation: |
  **Scannerクラスを使ぁE��備**には、以下�E2つのスチE��プが忁E��である�E�E

  1. **import斁E��Scannerクラスをインポ�EチE*
     ```java
     import java.util.Scanner;
     ```

  2. **Scannerオブジェクトを作�E**
     ```java
     Scanner scanner = new Scanner(System.in);
     ```

  **ポインチE*�E�E
  - `import`斁E�Eファイルの先頭�E�Eackageの次、classの前）に書ぁE
  - `System.in`は標準�E力を表ぁE
  - Scannerオブジェクト�E通常、変数名を`scanner`とする�E��E習！E
---

import { BlankInput } from '@site/src/components/question/inputs/BlankInput';
import { CodeBlock } from '@site/src/components/question/CodeBlock';

## 問顁E

以下�Eコード�E空欁E��埋めて、標準�E力を使ぁE��備を完�Eさせてください、E

<CodeBlock>
<BlankInput id="blank1" />
{`

public class Main {
    public static void main(String[] args) {
        `}<BlankInput id="blank2" />{` scanner = `}<BlankInput id="blank3" />{`;

        // ここから標準�E力が使える
    }
}`}
</CodeBlock>

**ヒンチE*�E�E
- import斁E��`import java.util.Scanner;`
- クラス名：`Scanner`
- オブジェクト作�E�E�`new Scanner(System.in)`

---

## 解説

標準�E力を使ぁE��は、E*Scannerクラスをインポ�Eトして、Scannerオブジェクトを作�Eする**忁E��がある、E

**手頁E�E詳細**�E�E

1. **import斁E��記述**
   ```java
   import java.util.Scanner;  // ファイルの先頭
   ```
   - `java.util`パッケージの`Scanner`クラスを使ぁE��とを宣言

2. **Scannerオブジェクトを作�E**
   ```java
   Scanner scanner = new Scanner(System.in);
   ```
   - `Scanner`�E�クラス吁E
   - `scanner`�E�変数名（任意だが、小文字で始める�Eが�E習！E
   - `new Scanner(System.in)`�E�標準�E力を読み取るScannerオブジェクトを作�E

import { OneCompilerCodeBlock } from '@site/src/components/OneCompilerCodeBlock';

<OneCompilerCodeBlock language="java" code={`import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Scannerオブジェクトを作�E
        Scanner scanner = new Scanner(System.in);

        // 標準�E力を使った�E琁E
        System.out.print("メチE��ージを�E力してください: ");
        String message = scanner.nextLine();

        System.out.println("受け取ったメチE��ージ: " + message);
    }
}`} />

**よくある間違ぁE*�E�E
- ❁E`import Scanner;`�E�パチE��ージ名が忁E��E��E
- ✁E`import java.util.Scanner;`
- ❁E`Scanner scanner = Scanner(System.in);`�E�Enew`が忁E��E��E
- ✁E`Scanner scanner = new Scanner(System.in);`
