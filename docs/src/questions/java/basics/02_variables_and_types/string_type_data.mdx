---
id: "java/basics/02_variables_and_types#string_type_data"
title: "String型が扱えるデータを説明できる"
type: "KNOW"
difficulty: "Easy"
format: "multipleChoice"
topicId: "02_variables_and_types"
category: "java/basics"
multipleSelect: false
choices:
  - id: "A"
    text: "整数（例：42, -100）"
  - id: "B"
    text: "小数（例：3.14, -0.5）"
  - id: "C"
    text: "文字列（例：\"Hello\", \"田中\"）"
answers:
  correct: ["C"]
explanation: |
  **String型**は、**文字列**（テキスト）を扱うための型です。

  ダブルクォート `"` で囲んだ文字の並びを保存できます。

  ```java
  String name = "田中太郎";
  String message = "Hello, World!";
  String empty = "";  // 空文字列
  ```

  **String型の特徴**：
  - 文字、単語、文章などのテキストデータを扱う
  - ダブルクォート `"` で囲む必要がある
  - 日本語も扱える
  - 数字も文字列として扱える（`String age = "25";` は文字列の "25"）

  **注意**：
  - `String x = Hello;` はエラー（ダブルクォートが必要）
  - `String x = 'Hello';` もエラー（シングルクォートは1文字用）
---

String型が扱えるデータとして正しいものを選べ。
