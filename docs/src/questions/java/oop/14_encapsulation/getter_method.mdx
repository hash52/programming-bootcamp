---
id: "java/oop/14_encapsulation#getter_method"
title: "getterで値を取得するメソッドを作れる"
type: "WRITE"
difficulty: "Easy"
format: "fillInBlank"
topicId: "14_encapsulation"
category: "java/oop"
fillInBlankAnswers:
  blank1: ["return name", "return this.name"]
sampleAnswer: "return name"
explanation: |
  getter メソッドは、privateフィールドの値を外部に返すためのメソッドである。慣例として `get + フィールド名（先頭大文字）` の命名にする（例: `getName()`）。戻り値の型はフィールドの型と一致させる。
---

import { BlankInput } from '@site/src/components/question/inputs/BlankInput';
import { CodeBlock } from '@site/src/components/question/CodeBlock';

以下のコードの空欄を埋めて、nameフィールドのgetterメソッドを完成させよ。

<CodeBlock>
{`class Person {
    private String name;

    public String getName() {
        `}<BlankInput id="blank1" />{`;
    }
}`}
</CodeBlock>
