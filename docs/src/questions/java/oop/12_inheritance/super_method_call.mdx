---
id: "java/oop/12_inheritance#super_method_call"
title: "superで親のメソッドを呼び出せる"
type: "WRITE"
difficulty: "Medium"
format: "fillInBlank"
topicId: "12_inheritance"
category: "java/oop"
hint: |
  `super.メソッド名()` で親クラスのメソッドを呼び出せる。オーバーライドしたメソッド内から親の処理を利用したい場合に使う。
fillInBlankAnswers:
  blank1: ["super.greet()", "super.greet( )"]
sampleAnswer: "super.greet()"
explanation: |
  `super.メソッド名()` で親クラスのメソッドを直接呼び出せる。オーバーライドしたメソッド内で親の処理も実行したい場合に使用する。
---

import { BlankInput } from '@site/src/components/question/inputs/BlankInput';
import { CodeBlock } from '@site/src/components/question/CodeBlock';

以下のコードの空欄を埋めて、子クラスのgreetメソッド内で親クラスのgreetメソッドを呼び出せ。

<CodeBlock>
{`class Person {
    void greet() {
        System.out.println("こんにちは");
    }
}

class Student extends Person {
    void greet() {
        `}<BlankInput id="blank1" />{`;
        System.out.println("学生です");
    }
}`}
</CodeBlock>
