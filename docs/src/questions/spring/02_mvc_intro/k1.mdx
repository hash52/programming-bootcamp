# MVCモデルの基本 — 演習問題

---

## 🧠 KNOW（知識確認）

### KNOW-1：MVCモデルの3つの役割（Model / View / Controller）を説明できる

**問題：**  
次の表の（A）（B）（C）を埋めよ。

| 役割 | 主な役目 |
|------|-----------|
| Model | （A） |
| View | （B） |
| Controller | （C） |

**回答：**  
- （A）アプリの処理・データを扱う（ビジネスロジック）  
- （B）ユーザーに見せる画面を作る（表示）  
- （C）入力を受け取り、ModelとViewを橋渡しする（制御）

**解説：**  
MVCはアプリを3つの役割に分けて構築する設計手法であり、Springはこれをベースに動作している。

---

### KNOW-2：MVCでModel・View・Controllerを分ける目的を理解している

**問題：**  
なぜ1つのクラスに全部の処理を書くより、Model・View・Controllerに分けるべきなのか、  
主な理由を2つ挙げよ。

**回答：**  
- 変更や修正の影響範囲を小さくできる（保守性が上がる）  
- デザインとロジックを分離して分業しやすくなる

**解説：**  
処理の責務を分けることで、UI変更やロジック修正を独立して行える。  
結果として保守性・再利用性・テストのしやすさが向上する。

---

### KNOW-3：MVCモデルを使うメリットを3つ以上挙げられる

**問題：**  
MVCモデルを採用することで得られるメリットを3つ挙げよ。

**回答：**  
1. 役割分担が明確になる  
2. 再利用性が高まる（Modelのロジックを他画面でも使える）  
3. テストがしやすくなる（ロジック単体で検証可能）  

**解説：**  
「見た目」と「処理」を分けることで、設計が整理され、チーム開発にも向く構造になる。

---

### KNOW-4：フレームワークが共通処理を肩代わりする意味を説明できる

**問題：**  
フレームワーク（例：Spring）が「共通処理」を肩代わりするとは、どういう意味か説明せよ。

**回答：**  
開発者が毎回書く必要があるログ出力・トランザクション管理・エラー処理などを、  
フレームワークが自動で行ってくれること。

**解説：**  
フレームワークが共通処理を吸収することで、開発者はビジネスロジックの実装に集中できる。  
これが「生産性が上がる」「安全に作れる」理由である。

---

### KNOW-5：AOP（アスペクト指向プログラミング）の役割を理解している

**問題：**  
AOP（アスペクト指向プログラミング）とはどのような仕組みか、簡潔に説明せよ。

**回答：**  
共通処理（ログ・例外・トランザクション管理など）をまとめて定義し、  
必要な箇所へ自動的に差し込む仕組み。

**解説：**  
AOPにより、全メソッドに重複して書いていた共通処理を1か所にまとめられる。  
Springの「@Transactional」などがその代表例。

---

## 👀 READ（コード理解）

### READ-1：MVCの流れ図を見てユーザー操作から画面表示までの処理順を説明できる

**問題：**  
次の流れのうち、正しい処理順を選べ。

> A. View → Controller → Model  
> B. Controller → View → Model  
> C. ユーザー → Controller → Model → View  

**回答：**  
C. ユーザー → Controller → Model → View

**解説：**  
ユーザー操作でControllerが呼ばれ、Modelが処理を行い、結果をViewに渡して画面を生成する。

---

### READ-2：悪例のコードからMVCが崩れている点を指摘できる

**問題：**  
以下のようなコードがMVCの原則に反している理由を説明せよ。

```java
public String handle(int hours, int hourlyWage) {
    int total = hours * hourlyWage;
    return "<html><body>給料は " + total + " 円です</body></html>";
}
```

**回答：**  
計算処理（Model）と画面生成（View）が1つのメソッドに混在しているため、責務が分離されていない。

**解説：**  
ロジックと見た目を分離しないと、デザイン変更やロジック修正のたびに同じ箇所を修正する必要が出てしまう。

---

## ✍️ WRITE（実装）

### WRITE-1：Model・View・Controllerを分けて処理を整理したJavaコードを記述できる

**問題：**  
勤務時間と時給から給与を計算してHTMLを返すプログラムを、  
Model・View・Controllerの3クラスに分けて書け。

**回答：**

```java
// Model
public class PayrollService {
    public int calculateSalary(int hours, int wage) {
        return hours * wage;
    }
}

// View
public class View {
    public static String render(int salary) {
        return "<p>給与は " + salary + " 円です</p>";
    }
}

// Controller
public class PayrollController {
    private final PayrollService service = new PayrollService();
    public String handle(int hours, int wage) {
        int salary = service.calculateSalary(hours, wage);
        return View.render(salary);
    }
}
```

**解説：**  
Controllerは入力と出力の制御、Modelは処理、Viewは表示を担当。  
それぞれを分けることでコードの責務が明確になり、保守性が高まる。
