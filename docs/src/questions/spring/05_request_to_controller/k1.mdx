# リクエストからコントローラへの値の受け渡し — 演習問題

---

## 🧠 KNOW（知識確認）

### KNOW-1：@RequestParam の仕組みと用途を理解している

**問題：**  
次のURLにアクセスしたとき、`@RequestParam("user")` で受け取れる値はどれか。

```
http://localhost:8080/show?user=Taro&age=20
```

**回答：**  
`Taro`

**解説：**  
クエリパラメータの `?user=Taro` が `@RequestParam("user")` に対応する。  
同様に `@RequestParam("age")` は `20` を受け取る。

---

### KNOW-2：@PathVariable の仕組みと用途を理解している

**問題：**  
次のコードで `/users/5` にアクセスすると、`id` の値はいくつとして受け取られるか。

```java
@GetMapping("/users/{id}")
public String show(@PathVariable("id") int id) {
    return "user";
}
```

**回答：**  
`5`

**解説：**  
URLの `/users/{id}` の `{id}` 部分に指定された値が `@PathVariable` に渡される。  
`/users/5` なら `id` = 5。

---

### KNOW-3：クエリパラメータとパスパラメータの使い分けを説明できる

**問題：**  
次のうち正しい説明を選べ。

1. パスパラメータはリソースを特定するために使う。  
2. クエリパラメータは検索条件や表示方法を指定するために使う。  
3. 両方同じ用途なので使い分ける必要はない。

**回答：**  
1 と 2

**解説：**  
- パスパラメータ：対象リソースを一意に特定する（例：`/items/1`）  
- クエリパラメータ：同じページ内の表示条件を指定する（例：`/items?sort=price`）

---

## 👀 READ（コード理解）

### READ-1：クエリパラメータを使ったリクエストをコードから読み取れる

**問題：**  
次のコードにおいて、ブラウザからどのようなリクエストを送ると `"Taro"` が出力されるか。

```java
@GetMapping("/show")
public String show(@RequestParam("user") String user) {
    System.out.println(user);
    return "display";
}
```

**回答：**  
`http://localhost:8080/show?user=Taro`

**解説：**  
`@RequestParam("user")` は `?user=...` の値を受け取る。  
`?user=Taro` の形式でURLに付与することで、`user` 変数に `"Taro"` が渡される。

---

### READ-2：パスパラメータを使ったリクエストをコードから読み取れる

**問題：**  
次のコードが出力するメッセージを答えよ。

```java
@GetMapping("/users/{id}")
public String detail(@PathVariable("id") int id) {
    System.out.println("ユーザーIDは " + id + " です。");
    return "user-detail";
}
```

ブラウザのURL:  
`http://localhost:8080/users/3`

**回答：**  
`ユーザーIDは 3 です。`

**解説：**  
`@PathVariable("id")` は `/users/{id}` の `{id}` に埋め込まれた値を取得する。

---

## ✍️ WRITE（実装）

### WRITE-1：@RequestParam を使ってクエリパラメータを受け取るコントローラを実装できる

**問題：**  
次のURLにアクセスしたときに、  
`"こんにちは Taro さん（20歳）"` と出力されるようなコントローラを作成せよ。

```
http://localhost:8080/greet?username=Taro&age=20
```

**回答：**

```java
@Controller
public class GreetController {

    @GetMapping("/greet")
    public String greet(
        @RequestParam("username") String username,
        @RequestParam("age") int age
    ) {
        System.out.println("こんにちは " + username + " さん（" + age + "歳）");
        return "result";
    }
}
```

**解説：**  
クエリパラメータを `@RequestParam` で受け取る。  
キー名と変数名を一致させると読みやすくなる。

---

### WRITE-2：@PathVariable を使ってパスパラメータを受け取るコントローラを実装できる

**問題：**  
次のURLにアクセスしたときに `"商品ID: 10"` と出力されるコントローラを作成せよ。

```
http://localhost:8080/items/10
```

**回答：**

```java
@Controller
public class ItemController {

    @GetMapping("/items/{id}")
    public String showItem(@PathVariable("id") int id) {
        System.out.println("商品ID: " + id);
        return "item-detail";
    }
}
```

**解説：**  
URLパスの一部に `{id}` を定義し、`@PathVariable` で値を受け取る。  
クエリではなくURL構造で識別する場合に有効。

---

### WRITE-3：フォームオブジェクトを使って複数の入力値をまとめて受け取るコントローラを実装できる

**問題：**  
次のフォーム送信を受け取り、  
`"こんにちは Hanako さん（25歳）"` と出力するコントローラを作成せよ。

```html
<form action="/submit" method="post">
  <input type="text" name="username">
  <input type="number" name="age">
  <button>送信</button>
</form>
```

**回答：**

```java
// フォームクラス
public class UserForm {
    private String username;
    private int age;
    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }
    public int getAge() { return age; }
    public void setAge(int age) { this.age = age; }
}

// コントローラ
@Controller
public class SubmitController {

    @PostMapping("/submit")
    public String submit(UserForm form) {
        System.out.println("こんにちは " + form.getUsername() + " さん（" + form.getAge() + "歳）");
        return "result";
    }
}
```

**解説：**  
フォームオブジェクトを使うと、複数項目をまとめて受け取れる。  
`<input name="username">` と `UserForm.username` が自動で対応付けられる。
