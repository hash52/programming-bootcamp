import { CodeBlockWithDiff } from "@site/src/components/CodeBlockWithDiff";

# Web開発の基礎知識（HTTP・ブラウザ・サーバー）

<CodeBlockWithDiff code={`
public String greet(String name) {
-   return "Hello, " + name;
+   // null安全にする
+   var safe = (name == null ? "World" : name);
+   return "Hello, " + safe + "!";
}`} language="java" startLineNumber={42} />

//tymeleaf diff サンプル
<CodeBlockWithDiff code={`<!DOCTYPE html  xmlns:th="http://www.thymeleaf.org">
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>サンプル</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
+  <h1 th:text="'Hello, ' + \${name} + '!'">Hello, World!</h1>
</body>
</html>
`} language="html" />

```sql
SELECT * FROM users WHERE id = 1;
```

```js
console.log("Hello, World!");
```

```java showLineNumbers showLineNumbers
@Controller
public class UserController {

    @PostMapping("/register")
    public String register(
        @Validated UserForm form,
        BindingResult bindingResult,
        Model model) {

        if (bindingResult.hasErrors()) {
            return "registerForm";
        }

        model.addAttribute("msg", "登録完了");
        return "result";
    }
}
```